import { FsListenerDirective } from './fs-listener.directive';

describe('FsListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new FsListenerDirective();
    expect(directive).toBeTruthy();
  });
});

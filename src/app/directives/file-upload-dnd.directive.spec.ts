import { FileUploadDNDDirective } from './file-upload-dnd.directive';

describe('FileUploadDNDDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadDNDDirective();
    expect(directive).toBeTruthy();
  });
});
